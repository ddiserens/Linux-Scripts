#!/bin/bash 
# "SEC_Check.sh" - Daniel Diserens <diserens@gdls.com>
# Version 1.0
################################################################################
#  DESCRIPTION: This script will modify inittab to require a password before   #
#  booting into single user mode.                                              #
################################################################################
#  NOTE:  Several variables are set and not used in this code.  This was done  #
#         intentionally, as I would like to make improvements later.           #
################################################################################
# set -x

#*******************************************************************************

#Variables
#. "${DIR}/lib/setup.lib"
#set_variables

#*******************************************************************************

#Functions
SEC_Check ()
{
RUN_TYPE="Check"
if [ ! -f ${CHECK_DIR} ];then
	mkdir -p ${CHECK_DIR} 
fi
SCRIPT="/root/Desktop/stig/test/020_check_passwd_single_user.sh" 
if [ -x "${SCRIPT}" ]; then
	${SCRIPT} $1 
fi

SCRIPT="/root/Desktop/stig/test/100_check_system_release.sh"
if [ -x "${SCRIPT}" ]; then
        ${SCRIPT} $1
fi

SCRIPT="/root/Desktop/stig/test/241_check_services.sh"
if [ -x "${SCRIPT}" ]; then
        ${SCRIPT} $1
fi

SCRIPT="/root/Desktop/stig/test/250_check_permissions.sh"
if [ -x "${SCRIPT}" ]; then
        ${SCRIPT} $1
fi

SCRIPT="/root/Desktop/stig/test/252_check_permissions.sh"
if [ -x "${SCRIPT}" ]; then
        ${SCRIPT} $1
fi

SCRIPT="/root/Desktop/stig/test/380_check_GID_passwd_file.sh"
if [ -x "${SCRIPT}" ]; then
        ${SCRIPT} $1
fi

SCRIPT="/root/Desktop/stig/test/400_check_banner.sh"
if [ -x "${SCRIPT}" ]; then
        ${SCRIPT} $1
fi

SCRIPT="/root/Desktop/stig/test/450_check_simultaneous_login.sh"
if [ -x "${SCRIPT}" ]; then
        ${SCRIPT} $1
fi

SCRIPT="/root/Desktop/stig/test/460_check_system_auth.sh"
if [ -x "${SCRIPT}" ]; then
        ${SCRIPT} $1
fi

SCRIPT="/root/Desktop/stig/test/500_check_screen_saver.sh"
if [ -x "${SCRIPT}" ]; then
        ${SCRIPT} $1
fi

SCRIPT="/root/Desktop/stig/test/540_check_login_def.sh"
if [ -x "${SCRIPT}" ]; then
        ${SCRIPT} $1
fi

SCRIPT="/root/Desktop/stig/test/590_check_passwd_hash.sh"
if [ -x "${SCRIPT}" ]; then
        ${SCRIPT} $1
fi
}
